doctype html
html
    head
        meta(name="viewport", content="width=device-width,initial-scale=1.0")
        title #{gallery}
        link(rel='stylesheet' href='/css/styles.css')
    body
        div.header.hero.is-dark.is-small
            div.hero-head
                div.container.has-text-centered-mobile.block
                    h1.title.is-2 #{gallery}
                    h2.subtitle.is-3 #{category}
        div.container
            div.videos
                div.columns.is-multiline.is-flex-direction-row
                    each file in videos
                        div.column.is-4
                            div.video.has-text-centered.has-border
                                div.block
                                    if file.thumbnail
                                        a(href=file.url)
                                            img(src=file.thumbnail).image.m-auto
                                div.block
                                    a.button.is-medium.is-link(href=file.url) Play
                                div.caption
                                    div.subtitle.is-5 #{file.name}
